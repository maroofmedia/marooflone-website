---
title: Quick Links | Maroof Lone
layout: 'base.njk'
---

<div class="container links-wrapper">
  
  <div class="links-profile">
    <div class="header-flex">
      <div class="square-avatar-container">
        <img src="/assets/images/main-images/marooflone-hero-image.webp" alt="Maroof Lone" class="square-avatar">
        
        <div class="avatar-overlay">
          <span>One Man Army</span>
        </div>

        <div class="online-dot"></div>
      </div>

      <div class="profile-text">
        <h1>Maroof Lone</h1>
        <p class="links-bio">Learning, building, and breaking things (responsibly).</p>
      </div>
    </div>
  </div>

  <div class="links-grid">
    
    <a href="/blog/the-chinar-tree-doesnt-panic-when-it-loses-its-leaves/" class="link-card">
      <div class="link-icon"><i class="fa-solid fa-pen-nib"></i></div>
      <div class="link-content">
        <h3>The Chinar Tree Doesn’t Panic When It Loses Its Leaves</h3>
        <p>Blog Post</p>
      </div>
      <div class="link-arrow"><i class="fa-solid fa-arrow-right"></i></div>
    </a>

    <a href="https://shifa.marooflone.com" target="_blank" class="link-card">
      <div class="link-icon"><i class="fa-solid fa-heart-pulse"></i></div>
      <div class="link-content">
        <h3>Heal Your Heart</h3>
        <p>AI-Powered Spiritual App</p>
      </div>
      <div class="link-arrow"><i class="fa-solid fa-arrow-right"></i></div>
    </a>

    <a href="mailto:hello@marooflone.com" class="link-card">
      <div class="link-icon"><i class="fa-solid fa-envelope"></i></div>
      <div class="link-content">
        <h3>Email Me</h3>
        <p>Get in touch directly</p>
      </div>
      <div class="link-arrow"><i class="fa-solid fa-arrow-right"></i></div>
    </a>

  </div>

</div>

<style>
.links-wrapper {
  max-width: 520px;
  margin: 0 auto;
  padding: 40px 20px 80px 20px;
  position: relative;
  z-index: 2;
}

/* --- Compact Header --- */
.header-flex {
  display: flex;
  align-items: center;
  gap: 20px;
  text-align: left;
  margin-bottom: 2.5rem;
}

.square-avatar-container {
  position: relative;
  flex-shrink: 0;
  cursor: pointer;
  line-height: 0;
}

.square-avatar {
  width: 90px;
  height: 90px;
  display: block;
  object-fit: cover;
  border-radius: 18px;
  border: 1px solid var(--border-color);
  background: var(--card-bg);
  box-shadow: 0 10px 30px -10px rgba(0,0,0,0.5);
  transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), border-color 0.3s ease;
}

.profile-text h1 {
  font-size: 1.5rem;
  margin: 0;
  color: #fff;
  line-height: 1.2;
}

.links-bio {
  color: var(--text-muted);
  font-size: 0.9rem;
  line-height: 1.4;
  margin-top: 6px;
}

/* --- Avatar Overlay --- */
.avatar-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  border-radius: 18px;
  background: rgba(8, 9, 10, 0.85);
  backdrop-filter: blur(3px);
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: 5;
}

.avatar-overlay span {
  color: var(--accent);
  font-weight: 800;
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 1px;
  line-height: 1.2;
  padding: 0 10px;
  display: block;
  width: 100%;
  transition: transform 0.3s ease;
}

.online-dot {
  position: absolute;
  bottom: -2px;
  right: -2px;
  width: 14px;
  height: 14px;
  background-color: #10B981;
  border: 2px solid var(--primary);
  border-radius: 50%;
  box-shadow: 0 0 10px rgba(16, 185, 129, 0.4);
  animation: pulse-green 2s infinite;
  z-index: 10;
}

.square-avatar-container:hover .square-avatar {
  transform: scale(1.05);
  border-color: var(--accent);
}

.square-avatar-container:hover .avatar-overlay {
  opacity: 1;
}

/* --- Links Grid --- */
.links-grid {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.link-card {
  display: flex;
  align-items: flex-start;
  padding: 16px 20px;
  background: var(--card-bg);
  border: 1px solid var(--border-color);
  border-radius: 16px;
  text-decoration: none;
  transition: all 0.2s ease;
}

.link-card:hover {
  transform: translateX(4px);
  border-color: var(--accent);
  background: #161b22;
  box-shadow: 0 4px 20px rgba(79, 209, 197, 0.05);
}

.link-icon {
  width: 42px;
  height: 42px;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
  color: var(--text-muted);
  margin-right: 16px;
  transition: all 0.2s;
  margin-top: 2px;
  margin-right: 16px;
  flex-shrink: 0;
}


.link-card:hover .link-icon {
  color: var(--accent);
  background: rgba(79, 209, 197, 0.1);
}

.link-content h3 {
  margin: 0;
  font-size: 1rem;
  font-weight: 600;
  color: #e5e7eb;
}

.link-content p {
  margin: 2px 0 0 0;
  font-size: 0.8rem;
  color: var(--text-muted);
}

.link-card:hover .link-content h3 {
  color: #fff;
}

.link-arrow {
  margin-top: auto;
  margin-bottom: auto;
  margin-left: auto;
  color: var(--text-muted);
  font-size: 0.8rem;
  opacity: 0;
  transform: translateX(-10px);
  transition: all 0.2s ease;
}

.link-card:hover .link-arrow {
  opacity: 1;
  transform: translateX(0);
  color: var(--accent);
}

@keyframes pulse-green {
  0% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.7); }
  70% { box-shadow: 0 0 0 6px rgba(16, 185, 129, 0); }
  100% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0); }
}

/* --- Mobile Fixes --- */
@media (max-width: 600px) {
  .links-wrapper {
    padding-top: 25px;
  }

  .header-flex {
    flex-direction: row;
    align-items: center;
    gap: 18px;
    margin-bottom: 2rem;
  }

  .square-avatar {
    width: 80px;
    height: 80px;
  }

  .profile-text h1 { font-size: 1.3rem; }
  
  .links-bio { 
    font-size: 0.85rem;
    margin-top: 4px;
  }

  .link-card {
    padding: 14px 16px;
  }
}
</style>