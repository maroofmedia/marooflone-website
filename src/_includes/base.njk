<!DOCTYPE html>
<html lang="en">
<head>
  <script>
    (function() {
      try {
        var savedTheme = localStorage.getItem('theme');
        var theme = savedTheme === 'light' ? 'light' : 'dark';
        document.documentElement.setAttribute('data-theme', theme);
      } catch (e) {}
    })();
  </script>
  <link rel="dns-prefetch" href="//www.googletagmanager.com">
  <link rel="dns-prefetch" href="//cdnjs.cloudflare.com">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> {{ title | default("Maroof Lone | Code, Context & Curiosity") }} </title>
  <meta name="description" content="{{ (description | default('Computer Science Engineer, Web Developer and Blogger from Kashmir. Specializing in OSINT, cybersecurity, and digital research solutions.')) | escape }}">
  <meta name="author" content="Maroof Lone">
  <meta name="generator" content="Eleventy">
  <meta name="theme-color" content="#08090A">
  <link rel="canonical" href="{{ site.url | default('https://www.marooflone.com') }}{{ page.url }}">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Jost:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500&display=swap" rel="stylesheet">
  <style>
    {{ "src/style.css" | inlineAndMinifyCss | safe }}
  </style>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" media="print" onload="this.media='all'">
  <noscript>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  </noscript>
  
  {% if page.url and '/blog/' in page.url %}
  <link rel="stylesheet" href="/assets/prism.css">
{% endif %}
  
  <link rel="apple-touch-icon" sizes="180x180" href="/assets/images/favicon/apple-touch-icon.png?v=3">
  <link rel="icon" type="image/png" sizes="192x192" href="/assets/images/favicon/android-chrome-192x192.png?v=3">
  <link rel="icon" type="image/png" sizes="32x32" href="/assets/images/favicon/favicon-32x32.png?v=3">
  <link rel="icon" type="image/png" sizes="16x16" href="/assets/images/favicon/favicon-16x16.png?v=3">
  <link rel="manifest" href="/assets/images/favicon/site.webmanifest?v=3">
   
  <!-- Open Graph Meta Tags -->
  <meta property="og:title" content="{{ (title | default('Maroof Lone | Code, Context & Curiosity')) | escape }}">
  <meta property="og:site_name" content="Maroof Lone">
  <meta property="og:description" content="{{ (description | default('Computer Science Engineer, Web Developer and Blogger from Kashmir. Specializing in OSINT, cybersecurity, and digital research solutions.')) | escape }}">
  <meta property="og:image" content="{{ site.url }}{% if image %}{{ image }}{% else %}/assets/images/main-images/og-discription-image.png?v=2{% endif %}">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  <meta property="og:image:alt" content="{{ title | default('Maroof Lone | Code, Context & Curiosity') }}">

{% if page.url %}
{% if tags and "post" in tags %}
    <meta property="og:type" content="article">
  {% else %}
    <meta property="og:type" content="website">
  {% endif %}
{% else %}
  <meta property="og:type" content="website">
{% endif %}

{% if noindex %}
    <meta name="robots" content="noindex, follow">
  {% else %}
    <meta name="robots" content="index, follow">
  {% endif %}

<!-- Twitter Card Meta Tags -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:image:width" content="1200">
<meta name="twitter:image:height" content="630">
<meta name="twitter:title" content="{{ (title | default('Maroof Lone | Code, Context & Curiosity')) | escape }}">
<meta name="twitter:description" content="{{ (description | default('Computer Science Engineer, Web Developer and Blogger from Kashmir. Specializing in OSINT, cybersecurity, and digital research solutions.')) | escape }}">
<meta name="twitter:image" content="{{ site.url }}{% if image %}{{ image }}{% else %}/assets/images/main-images/og-discription-image.png?v=2{% endif %}">
<meta name="twitter:image:alt" content="{{ title | default('Maroof Lone | Code, Context & Curiosity') }}">

<!-- Google tag (gtag.js) - Deferred loading to improve initial PageSpeed and eliminate unused JS -->
<script>
  (function() {
    let analyticsLoaded = false;

    function loadAnalytics() {
      if (analyticsLoaded) return;
      analyticsLoaded = true;

      // Remove the interaction event listeners
      ['mouseover', 'keydown', 'touchmove', 'touchstart', 'scroll'].forEach(function(event) {
        window.removeEventListener(event, loadAnalytics);
      });

      // Dynamically load Google Tag Manager script
      const script = document.createElement('script');
      script.async = true;
      script.src = 'https://www.googletagmanager.com/gtag/js?id=G-T20K3YKJP9';
      document.head.appendChild(script);

      // Initialize the dataLayer and config call
      window.dataLayer = window.dataLayer || [];
      window.gtag = function() { window.dataLayer.push(arguments); };
      window.gtag('js', new Date());
      window.gtag('config', 'G-T20K3YKJP9');
    }

    // Set up listeners for the first user interaction
    ['mouseover', 'keydown', 'touchmove', 'touchstart', 'scroll'].forEach(function(event) {
      window.addEventListener(event, loadAnalytics, { passive: true });
    });

    // Fallback: load after 4 seconds of inactivity if no interaction has occurred
    setTimeout(loadAnalytics, 4000);
  })();
</script>

<!-- Netlify CMS — only load on admin pages -->
{% if page.url and '/admin/' in page.url %}
 <script defer src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
{% endif %}


<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "name": "Maroof Lone",
      "url": "https://www.marooflone.com/"
    },
    {
      "@type": "Person",
      "name": "Maroof Lone",
      "jobTitle": "Computer Science Engineer, Web Developer & Blogger",
      "url": "https://www.marooflone.com/",
      "image": "https://www.marooflone.com/assets/images/main-images/marooflone-hero-image.webp",
      "description": "Computer Science Engineer, Web Developer, and Blogger hailing from Kashmir. Specializing in OSINT, cybersecurity, and digital research.",
      "sameAs": [
        "https://www.linkedin.com/in/marooflone13",
        "https://github.com/maroofmedia",
        "https://twitter.com/marooflone13",
        "https://www.instagram.com/marooflone13",
        "https://share.google/jHH04LLUWHG9WncGx"
      ]
    },
    {
      "@type": "WebPage",
      "@id": "{{ site.url | default('https://www.marooflone.com') }}{{ page.url }}",
      "url": "{{ site.url | default('https://www.marooflone.com') }}{{ page.url }}",
      "name": "{{ title | default('Maroof Lone | Code, Context & Curiosity') | safe }}",
      "description": "{{ description | default('Computer Science Engineer, Web Developer and Blogger from Kashmir. Specializing in OSINT, cybersecurity, and digital research solutions.') | safe }}"
    }
  ]
}
</script>

<link rel="alternate" href="/feed.xml" title="Maroof Lone's Blog" type="application/atom+xml">

{% if tags and "post" in tags %}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": "{{ site.url | default('https://www.marooflone.com') }}{{ page.url }}"
  },
  "headline": "{{ title | safe }}",
  "image": "{{ site.url | default('https://www.marooflone.com') }}{% if image %}{{ image }}{% else %}/assets/images/main-images/og-discription-image.png?v=2{% endif %}",
  "author": {
    "@type": "Person",
    "name": "{{ author | default('Maroof Lone') | safe }}",
    "url": "{{ site.url | default('https://www.marooflone.com') }}/"
  },
  "publisher": {
    "@type": "Organization",
    "name": "Maroof Lone",
    "logo": {
      "@type": "ImageObject",
      "url": "{{ site.url | default('https://www.marooflone.com') }}/assets/images/favicon/favicon-32x32.png"
    }
  },
  "datePublished": "{{ date | date('yyyy-MM-dd') }}",
  "dateModified": "{{ page.date.toISOString() }}"{% if description %},
  "description": "{{ description | safe }}"{% endif %}
}
</script>
{% endif %}

</head>
<body>
<a href="#main-content" class="skip-link">Skip to main content</a>
<div id="progress-bar"></div>
  <div class="page-wrapper">
    {% include 'header.njk' %}
    <main id="main-content">
      {{ content | safe }}
    </main>
    {% include 'footer.njk' %}
  </div>

  <script>
    // Netlify Identity
    if (window.netlifyIdentity) {
      window.netlifyIdentity.on("init", (user) => {
        if (!user) {
          window.netlifyIdentity.on("login", () => {
            document.location.href = "/admin/";
          });
        }
      });
    }

    // Header Scroll Behavior
    let lastScroll = 0;
    const header = document.getElementById('mainHeader');
    const threshold = 50;

    window.addEventListener('scroll', () => {
      const currentScroll = window.pageYOffset;
      
      // Only trigger if scrolled more than threshold
      if (Math.abs(currentScroll - lastScroll) < 10) return;

      if (currentScroll > lastScroll && currentScroll > threshold) {
        // Scroll Down
        header.classList.add('hidden');
      } else {
        // Scroll Up
        header.classList.remove('hidden');
      }
      lastScroll = currentScroll;
    });

    // Mobile Menu Toggle
    function toggleMobileMenu() {
      const header = document.getElementById('mainHeader');
      if (header) {
        header.classList.toggle('menu-open');
        document.body.classList.toggle('no-scroll', header.classList.contains('menu-open'));
      }
    }
    window.toggleMobileMenu = toggleMobileMenu;
    window.toggleMenu = toggleMobileMenu;

    // Close menu when clicking outside header
    document.addEventListener('click', (e) => {
      const header = document.getElementById('mainHeader');
      if (header && header.classList.contains('menu-open') && !header.contains(e.target)) {
        header.classList.remove('menu-open');
        document.body.classList.remove('no-scroll');
      }
    });

    // Close menu when clicking any navigation link
    document.querySelectorAll('.mobile-nav-link, .nav-link').forEach(link => {
      link.addEventListener('click', () => {
        const header = document.getElementById('mainHeader');
        if (header && header.classList.contains('menu-open')) {
          header.classList.remove('menu-open');
          document.body.classList.remove('no-scroll');
        }
      });
    });

    // Theme Management (Dark Mode Default + Manual Toggle)
    (function() {
      function getPreferredTheme() {
        const savedTheme = localStorage.getItem('theme');
        return savedTheme === 'light' ? 'light' : 'dark';
      }

      function updateThemeUI(theme) {
        document.documentElement.setAttribute('data-theme', theme);
        const btn = document.getElementById('themeToggleBtn');
        if (btn) {
          btn.setAttribute('title', theme === 'light' ? 'Switch to Dark Mode' : 'Switch to Light Mode');
          btn.setAttribute('aria-label', theme === 'light' ? 'Switch to Dark Mode' : 'Switch to Light Mode');
        }
      }

      window.toggleTheme = function() {
        const current = document.documentElement.getAttribute('data-theme') || getPreferredTheme();
        const next = current === 'light' ? 'dark' : 'light';
        localStorage.setItem('theme', next);
        updateThemeUI(next);
      };

      // Initial sync on page load
      updateThemeUI(document.documentElement.getAttribute('data-theme') || getPreferredTheme());
    })();

    // Scroll Animations
    const observerOptions = {
      root: null,
      rootMargin: '0px',
      threshold: 0.1
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('animated');
          observer.unobserve(entry.target);
        }
      });
    }, observerOptions);

    document.querySelectorAll('.animate-on-scroll').forEach(el => {
      observer.observe(el);
    });
  </script>

  <script>
  // Find all code blocks
  document.querySelectorAll('pre').forEach((pre) => {
    // 1. Create the button
    const button = document.createElement('button');
    button.className = 'copy-button';
    button.innerHTML = '<i class="far fa-copy"></i>';
    button.ariaLabel = 'Copy code';

    // 2. Add Click Event
    button.addEventListener('click', () => {
      const code = pre.querySelector('code').innerText;
      
      navigator.clipboard.writeText(code).then(() => {
        button.innerHTML = '<i class="fas fa-check"></i>';
        button.classList.add('copied');
        
        setTimeout(() => {
          button.innerHTML = '<i class="far fa-copy"></i>';
          button.classList.remove('copied');
        }, 2000);
      }).catch(err => {
        console.error('Failed to copy:', err);
      });
    });

    pre.appendChild(button);
  });
  </script>

  <script>
    // Only run on pages with /blog/ in the URL
    if (window.location.pathname.includes('/blog/')) {
      const progressBar = document.getElementById('progress-bar');
      
      window.addEventListener('scroll', () => {
        const scrollTop = document.documentElement.scrollTop || document.body.scrollTop;
        const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
        const scrolled = (scrollTop / scrollHeight) * 100;
        
        if (progressBar) {
          progressBar.style.width = scrolled + "%";
        }
      });
    }
  </script>


</body>
</html>
